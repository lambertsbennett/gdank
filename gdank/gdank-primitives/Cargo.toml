[package]
name = "gdank-primitives"
version = "0.1.0"
edition = "2024"

[dependencies]
serde =  {version = "1.0.228", features=["derive"]}

[dependencies.uuid]
version = "1.22.0"
# Lets you generate random UUIDs
features = [
    "v4",
]
